use genkit;

fn main() {
    println!("Running ollama_simple_embed sample...");
    // Placeholder - Add logic using genkit and potentially ollama plugin for embeddings
    let core_msg = genkit::hello();
    println!("Core genkit message: {}", core_msg);
    println!("ollama_simple_embed finished.");
}
