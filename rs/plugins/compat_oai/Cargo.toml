[package]
name = "genkit-plugin-compat-oai"
version = { workspace = true }
edition = { workspace = true }
authors = { workspace = true }
license = { workspace = true }
publish = false

[lib]
name = "genkit_plugin_compat_oai"
path = "src/lib.rs"

[dependencies]
genkit = { path = "../../genkit" }

# Add plugin-specific dependencies here (e.g., OpenAI client, http clients)
