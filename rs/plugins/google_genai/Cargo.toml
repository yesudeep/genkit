[package]
name = "genkit-plugin-google-genai"
version = { workspace = true }
edition = { workspace = true }
authors = { workspace = true }
license = { workspace = true }
publish = false

[lib]
name = "genkit_plugin_google_genai"
path = "src/lib.rs"

[dependencies]
genkit = { path = "../../genkit" }

# Add plugin-specific dependencies here (e.g., Google AI SDKs)
