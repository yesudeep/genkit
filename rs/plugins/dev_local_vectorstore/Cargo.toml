[package]
name = "genkit-plugin-dev-local-vectorstore"
version = { workspace = true }
edition = { workspace = true }
authors = { workspace = true }
license = { workspace = true }
publish = false

[lib]
name = "genkit_plugin_dev_local_vectorstore"
path = "src/lib.rs"

[dependencies]
genkit = { path = "../../genkit" }

# Add plugin-specific dependencies here (e.g., embedding libraries, local storage)
